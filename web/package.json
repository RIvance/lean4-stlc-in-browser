{
  "name": "lean-stlc-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.12" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "check": "tsc --noEmit",
    "build": "npm run check && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:wasm": "node --test tests/wasm.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@language-playground/ide": "file:../playground-core",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "8.3.0"
  }
}
